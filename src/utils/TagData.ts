import { FaReact } from "@react-icons/all-files/fa/FaReact"

const takeTagImage = (name: string) => {
  const image = name
  return image
}

const takeTagInfo = () => {
  const tagInfos = [
    {
      'icon': FaReact,
      'name': 'React',
    }
  ]
  return tagInfos
}

const toUrl = (name: string) => {
  const url = name
  return url
}
